trigger:
  branches:
    include:
      - main

pr:
  branches:
    include:
      - main

variables:
  terraformVersion: '1.7.5'
  azureServiceConnection: 'Azure-Service-Connection-Name'

stages:
  # Placeholder for Validate, Plan, Apply stages with approvals
  # Use the previous enterprise YAML content here
